{
  "name": "haviflow",
  "private": true,
  "version": "0.1.0",
  "description": "HaviFlow — Verify once with your face. Get a living roadmap to your driver's license.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "test": "npm --prefix backend run test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
